# TODO

Pending polish items for this portfolio site. Tracked here (instead of only in Claude's memory) so it's visible across every machine this repo is checked out on. Remove an item once it's actually implemented — don't just mark it done in prose.

- [ ] **Try different fonts.** Current fonts are Space Grotesk (`--font-heading`) and Inter (`--font-body`), set in `src/styles/global.css`. Explore alternatives.
- [ ] **Clean up GitHub project visibility, or remove the GitHub link.** `home.json` socials currently link to `https://github.com/joelberger76`. Either tidy up what's publicly visible on that profile, or drop the link.
- [ ] **Add a custom domain: `joelberger.dev`.** Site currently serves from the default `https://joelberger76.github.io` (GitHub Pages). Domain will be `joelberger.dev`, with `www.joelberger.dev` also supported (decide apex vs. `www` as the canonical/redirect target). Needs a `CNAME` file in `public/`, DNS records at the registrar, and `siteUrl` in `home.json` (plus `SITE_URL` env var if used in CI) updated to match. When this is picked up, provide walkthroughs for both sides: DNS record setup on Cloudflare (the DNS provider in use, covering both the apex and `www` records), and the corresponding GitHub Pages custom domain configuration (repo Settings > Pages, `CNAME` file, HTTPS enforcement). Porkbun is the registrar and DNS provider.
- [ ] **Add GoatCounter analytics, production only.** Not implemented anywhere yet. Should follow the same `import.meta.env.PROD` gating pattern `index.astro` already uses for the live-backend fetch, so it doesn't fire during local dev.
- [ ] **Give mobile a dedicated pass.** No specific bugs identified yet — just hasn't had a deliberate mobile review since recent layout changes (Education split, section subtitle removal, etc.). Check spacing, touch targets, and readability end-to-end on a real device or emulator.
- [ ] Consider replacing the mobile toolbar. No concrete complaint yet — just a hunch that the current bottom nav (`nav.astro`, icon+label bar that auto-hides on scroll) might not be the right pattern. Revisit once there's a specific reason to change it.
- [ ] **Update LinkedIn profile to match the site, and add the site URL.** Once the primary tasks in this list are done (site content/design settled, custom domain live), sync LinkedIn's info with what's on the portfolio site and add the site's URL (once the custom domain above is set up, use `https://joelberger.dev`) to the LinkedIn profile.
- [ ] **Enable "Enforce HTTPS" for the custom domain once available.** Repo Settings > Pages, after `joelberger.dev` DNS check succeeds. GitHub auto-provisions the TLS cert once DNS verifies, which can take up to ~24h after the DNS records are added at Porkbun, so this has to be a follow-up check, not something done in the same pass as the initial custom domain setup.
- [ ] **Visually validate look and feel at every breakpoint, site-wide.** Check each section (Home, Career, Education, Key Initiatives, Core Competencies, Contact, nav) at each Tailwind breakpoint (default/mobile, `sm` 640px, `md` 768px, `lg` 1024px, `xl` 1280px, `2xl` 1536px) for overflow, clipping, and awkward stretching, not just "does it look fine at a glance." Motivated by the Core Competencies bug just found and fixed: a flex `min-width: auto` default plus mismatched grid column breakpoints caused real, silent text clipping (no ellipsis) at specific in-between widths that wasn't obvious without actually resizing through each breakpoint and inspecting closely. Other sections (`career-card.astro`, `project-card.astro`, `education-card.astro`) use similar flex/grid layouts and haven't had the same scrutiny.